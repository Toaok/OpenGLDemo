package indi.toaok.matrix.units

/**
 * 方程求解工具
 */

/**
 * 计算方程ax+b=0的解
 */
fun calcLineFormulaZero(a: Double, b: Double) {
    val results = ArrayList<Double>()
    //最高次项系数为0，方程无解或有无穷解，此处忽略
    if(a!=0){
        results.add()
    }
}
